import { chromium } from "@playwright/test";
const b = await chromium.launch();
const p = await (await b.newContext()).newPage();
p.setDefaultTimeout(10000);
await p.goto("http://localhost:3100/");
await p.getByRole("button", { name: /Priya Sharma/ }).click();
await p.getByRole("textbox").first().fill("Verifier walk B");
await p.getByRole("button", { name: /Start assessment/ }).click();
await p.waitForLoadState("networkidle");
const url = p.url();
await p.getByLabel(/Business Purpose/).fill("Schedule shifts for warehouse staff.");
await p.getByLabel(/Activity \/ Use-Case/).fill("A vendor tool assigns shifts using historical demand.");
await p.getByLabel(/Does this use AI/).selectOption({ label: "Yes" });
await p.waitForTimeout(1200);
console.log("--- after AI=Yes ---");
console.log((await p.locator("main, form").first().innerText()).slice(0,2200));
await p.screenshot({ path: "/tmp/v-intake1b.png" });
console.log("PROJECT:", url);
await b.close();
