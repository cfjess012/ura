---
slice: S6
spec-version: 2026-08-23.1
verified-by: Claude (builder walk, browser-driven)
verified-on: 2026-08-23
---

# UAT — S6

**Done when:** §19 criteria; suppressed children invisible; N-A without justification impossible.

Every requirement this slice owns gets a row below. Fill each one with what
was actually done and actually observed — "works" is not evidence. A row
marked `fail` needs a follow-up note saying how it will be addressed.

### FR-12 — Tier-3 self-assessment: Yes/Partial/No/N-A; notes required on Partial/No/N-A

- **What was done:** Answered control objectives on Novara scheduling assistant with each of the four answers in turn, and submitted with a note missing.
- **What was observed:** Yes stands alone. Partial, No and N-A each reveal a note field, and N-A asks a different question — "Why doesn't this apply?" against "What exists today, and what is missing?". Submitting without one is refused, the savebar names how many are missing, and focus moves to the first offending field. The refusal is in the action, so a forged request is refused too.
- **Result:** pass
- **Evidence:** Browser walk 2026-08-23; `e2e/objectives.spec.ts` (6 journeys); `test/unit/tier3.test.ts` (18 tests) covers the pure rules.

### FR-13 — Child questions fire only on parent Yes, subject to cross-tier conditions

- **What was done:** Toggled a parent objective between all four answers and watched its children.
- **What was observed:** Children appear only on Yes and disappear entirely on Partial, No and N-A — not greyed, not "skipped", absent (§3.4). A child whose cross-tier condition is unmet never appears at all; the unit test drives one from unmet to met and watches it arrive.
- **Result:** pass
- **Evidence:** `e2e/objectives.spec.ts` "children appear only on Yes, and vanish again"; `test/unit/tier3.test.ts` conditional-child case.

### FR-21 — Notes/questions attachable at any point; travel to the reviewer

- **What was done:** Checked what a note does and where it goes.
- **What was observed:** Every non-Yes answer carries its note in the same record as the answer, insert-only and attributed. **Partly met:** the note travels with the answer and a reviewer will read it at S8, but "notes attachable at any point" — a free note on any question, anywhere — is not built. Only the answer-bound note exists.
- **Result:** fail
- **Evidence:** The stored value is `{answer, note}` (`isTier3Value`); no general note surface exists. Follow-up: FR-21's free-note half belongs with S8, where the reviewer surface that consumes it is built. Recorded rather than quietly claimed.

## Findings

**FR-21 is only half met and this record says so** — see its row. The
answer-bound note works; the "attachable anywhere" note does not exist. It
is deferred to S8 with the reviewer surface that would read it, not quietly
counted as done.

**Three defects found by walking, none visible to a unit test.** The store
flattened object answers to a string, so a Tier-3 answer saved and never
came back — `answers.value` is jsonb and always was, but the TYPE was
narrower than the column. The page used `.assess-layout`, whose first grid
column IS the rail, so a railless screen squeezed its content into 15rem.
And parent and child radio groups shared one accessible name, so a screen
reader announced a child's buttons as answering its parent.

**One found by the E2E database.** Three scripts bring an environment up —
the package script, the dev reset, the E2E prep — and each kept its own list
of seeds, so the new instrument reached dev and not e2e. A test now asserts
all three run every seed script.

**One found on the demo path.** The summary had two buttons both named
"Answer them", going to different stages. Renamed to say which.

**Content boundary, declared not discovered.** 15 of 51 objectives have
questions; 5 child questions were declined because their conditions
reference questions this instrument does not ask. Both stated in G-27's
terms and shown on screen.

## Agentic opportunity

This is the strongest surface in the product for §22.1's drafting
capability. A control question — "are access rights periodically reviewed
and recertified?" — is exactly the kind a policy document already answers,
and the note a person must write is exactly what a citation would ground.
An agent could propose Yes/Partial/No with the sentence it read and the
document it came from, and the person confirms or overrides.

Guardrails it needs: it proposes, never records — the click stays the
answer (G-39a). Every proposal carries a verbatim quote and its source; a
proposal it cannot ground is surfaced as a question, not an answer. It may
never write the note on the person's behalf, because the note is the
person's account of their own control.

Nothing here forecloses it: the answer and its note are stored together as
one value, so a drafted proposal has somewhere to put its evidence without
a schema change, and `source` already distinguishes how an answer arrived.

## Verifier

**RUN 2026-08-23. Verdict: FAIL** — three blocking findings, all fixed and
re-verified:

- **The action recorded any question id it was sent.** A forged request
  wrote an object into `gate.operational`, which the gate reader resolves to
  "No" — silently flipping an answer the person had given, and shifting the
  whole summary. It now authorises against what the assessment actually
  asks, derived from the record by the same function the screen renders
  from. Re-ran the verifier's three exploits: all refused, gate intact.
- **"N-A without justification impossible" was false** — this slice's own
  done-when. A child submitted under a non-Yes parent skipped the note rule
  entirely, because the check only validated children `childrenAsked`
  returned. Such a child is now not answerable at all.
- **Answers were lost on navigation.** This was the only assess screen
  without autosave, so an answer given here vanished with nothing said —
  the exact silent discard G-40a forbids. Autosave wired; an answer that
  still needs a note is deliberately NOT saved until it has one, because a
  bare "No" in the record is a gap with no explanation.

Also fixed from the same pass: the stage strip ticked "Assess" as done while
asking assessment questions; the page was a dead end with no route off it;
an undesigned empty state when severity is answered but nothing crosses a
threshold; "Tiers 1 and 2" in the opening sentence (§24.7 — that word
appears nowhere else in a person's journey); no helper text on any question
(§24.11) when the instrument already carries it and it was rendered
nowhere; and "1 still need a note".

## Not verified

- **A production build.** Everything was driven against `next dev`. The run
  sheet requires `pnpm build && pnpm start` and that has not been exercised.
- **Concurrency** — two tabs saving the same objectives page at once.

- **FR-21's free-note half** — not built, see above.
- The 36 objectives with no questions are listed on screen but nobody has
  read that list as a person and judged whether it reassures or alarms.
- Keyboard-only completion of a control question including its note field.
- Screen-reader output. Each radio group now carries its own question as its
  accessible name, but nothing was heard.
- Behaviour when a severity answer is REMOVED after control questions are
  answered — the objective should disappear and its answer become orphaned
  history. Reasoned from the derivation, never exercised.
- The note field has no length limit, the same gap the hand-off note has.
