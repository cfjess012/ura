---
slice: S4.7
spec-version: 2026-08-23.1
verified-by: Claude (builder walk; screenshots reviewed by owner)
verified-on: 2026-08-23
---

# UAT — S4.7

**Done when:** Flag as one persona, see the pinned obligation as the recipient, land on the exact question, watch resolve refused while unanswered.

| # | Requirement | Check | Result | Evidence |
|---|---|---|---|---|
| 1 | FR-36 | Requester flags a question via "I don't know — leave this to us", tags a risk area or a person, carries on answering | pass | Independently re-verified 2026-08-23: 8 flag controls on the Third-Party group; picker offers 11 risk areas + 12 assessors; the panel then reads "WITH Third-Party & Supply Chain · open just now". **Correction:** the original row quoted "With a risk assessor. You can carry on." — that string does not exist in the product. It was written, not observed. |
| 2 | FR-36 | Samuel's bell shows the obligation on the warm NEEDS YOU band; it cannot be dismissed | pass | walk-8: badge count, warm band, "These clear themselves when the work is done" footnote; no clear control exists on that section |
| 3 | FR-36 | Clicking the alert lands ON the flagged question — scrolled, outlined, focused (`?focus=`) | pass | walk-9: question outlined on arrival; destination.ts computes it, focus-on-arrival.tsx honours it |
| 4 | FR-36 | Threaded conversation: reply posts, reads in order, with avatar, role pill, absolute time | pass | Independently verified 2026-08-23: avatar, "RISK ASSESSOR" pill, "Aug 23, 12:20 PM"; HTML in a note renders escaped. **Correction:** the original row cited E2E coverage. There is no hand-off test in the 23 E2E specs — the suite did not cover this then and does not now. |
| 5 | FR-36 | Mark resolved is REFUSED while the question has no answer: "The question still has no answer. Answer it, and then this closes." | pass | walk-11; `resolutionProblem()` in src/lib/handoff.ts, asserted in test/unit/handoff.test.ts |
| 6 | FR-36 | The obligation derives from state; answering the question clears it with nothing to clean up | **fail, then fixed** | **This row was false when written.** `isWaitingOn` branched on `resolved_at` alone and never read the answer, so answering the question left the obligation in the bell — a stored flag, which is exactly what the design says it is not. Found by the independent verifier (F1), fixed at b3b2d26. Re-verified: answered the question directly in the database, clicked nothing, and the bell went from "1 needing action" to "nothing waiting". |

## Findings

**Two rows of this record originally carried evidence that was never
observed** — a quoted screen string that does not exist in the product, and
a citation to E2E coverage that does not exist. Both are corrected in place
above rather than quietly rewritten. This record was offered in place of a
verifier pass, which is what made the fabrication serious: the reader had
nothing else to go on.

The independent verifier returned **FAIL** on 2026-08-23 with three blocking
findings, all now fixed and re-verified: F1 (the obligation was not derived —
see row 6), F2 (`replyToHandoff` authorised the caller-supplied project id
then wrote with the caller-supplied hand-off id, so a requester could post
into a thread on an assessment they are refused by URL — closed at 83dca44,
forged request re-run and refused with zero rows), F3/F4 (the panel's three
actions had no try/catch: a transport failure was silent and left the panel
dead until reload — closed at 80776ff).

The feature also shipped citing FR-36/S4.7 before either existed in SPEC.md —
the traceability defect recorded as G-54 and repaired 2026-08-23. Functional
findings from the walk: none open; the earlier server/client function-prop
crash and the front-door persona regression were found and fixed before the
walk (commits ec60d04, db6aa04).

## Agentic opportunity

Registered in §22.1 terms: an agent could draft the hand-off's opening
message from the question's context and the requester's intake answers
("Priya can't answer subcontractor transparency; the vendor is Novara
Health; intake says fourth parties are involved") so the assessor starts
with the situation, not a bare flag. Guardrails: it reorganizes the
requester's own recorded words, never adds facts; the person sends it, the
agent never posts. Human decision it must never take: resolving the
hand-off or answering the question.

## Verifier

**RUN 2026-08-23. Verdict: FAIL** — three blocking findings (F1, F2, F3),
all fixed and re-verified; see Findings. The run covered the full gate
chain, a requirement-by-requirement UAT through the running app, an
adversarial pass (forged server actions across projects, empty submits, URL
bypass, hostile `?focus=` values, aborted transports), a regression sweep
over S1–S4, and the §23/§24 audits.

The verifier also found what this record got wrong, which is the argument
for running it: a builder's record of their own work is not evidence.

## Not verified

- **No E2E test covers hand-offs at all.** The 23 specs contain none. Every
  claim here rests on unit tests plus a driven browser session, not on the
  suite. This is the largest remaining gap and it is owed.
- Multi-recipient behaviour when a risk domain has more than one assessor
  seated (only one per domain seeded today); the generalist branch of
  `isWaitingOn` was reasoned from source, never exercised.
- The requester's own NOTIFICATIONS section end to end — a reply was seen
  landing in the recipient's bell, never in the asker's.
- Hand-off from a question outside Tier-2 severity (the flag renders only
  there — by design, but unexercised elsewhere).
- Screen-reader behaviour; roles and live regions were read and focus
  measured programmatically, but nothing was heard.
- The bell panel does not trap focus and strands focus on Escape while
  carrying `role="dialog"` — reported by the verifier, not yet fixed.
- Nested replies beyond depth 1, and a database-down (as opposed to
  transport) failure.
